on: push
name: testing
jobs:
  checks:
    name: run
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master

    - name: run
      uses: rajeshg007/mfa-auth/.github/actions/build@test
